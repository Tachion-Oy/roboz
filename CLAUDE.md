# Claude Code Instructions

Before planning or modifying this repository, read `AGENTS.md` in the repository
root completely and follow it for the entire task. `AGENTS.md` is the canonical
source of repository policy and takes precedence if these files ever appear
inconsistent.

Do not declare a change complete until its testing, typing, documentation,
docstring, changelog, packaging, distribution-validation, release-boundary, and
handoff requirements from `AGENTS.md` have been satisfied or any blocked checks
have been reported explicitly.
